<UserControl x:Class="PrismDemo.Menu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:prism="http://prismlibrary.com/"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="525">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Button Grid.Column="0" Content="Moduł biznesowy" VerticalAlignment="Top" Margin="5" Command="{Binding ViewMBCommand}"/>
        <Button Grid.Column="1" Content="ViewA" VerticalAlignment="Top" Margin="5" Command="{Binding ViewACommand}"/>
        <Button Grid.Column="2" Content="ViewB" VerticalAlignment="Top" Margin="5" Command="{Binding ViewBCommand}"/>
        
    </Grid>
</UserControl>
